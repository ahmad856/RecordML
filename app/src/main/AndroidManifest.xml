<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.recordml">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activities.RecordingsListView"/>

        <activity
            android:name=".activities.RecordingsTab" />

        <activity
            android:name=".activities.AddRecording" />

        <activity
            android:name=".activities.DownloadFiles" />

        <activity android:name=".activities.Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        <receiver android:name=".broascastrecievers.Download"-->
<!--            android:priority="1000">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />-->
<!--            </intent-filter>-->
<!--        </receiver>-->

<!--        <receiver android:name=".activities.RecordingsListView$Download"-->
<!--            android:priority="1000">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />-->
<!--            </intent-filter>-->
<!--        </receiver>-->

    </application>

</manifest>